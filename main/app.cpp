#include "./dfs/server/include/Server.hpp"

int main() {
  Server server;
  server.start();

  /* std::string var {"Hello World"}; */
  /* size_t charSize = sizeof(std::string); */
  /* size_t memoryAlloc = var.capacity() * sizeof(char); */
  /* std::cout << charSize + memoryAlloc << std::endl; */
  return 0;
}
